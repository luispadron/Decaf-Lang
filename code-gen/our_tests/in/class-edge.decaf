class Dog {
    int age;

	void test() {
	    this.test();
	}

    int math(int math) {
        this.math(0);
        math = 4;
    }

    void set_age(int age) {
        age = age * 10;
        this.age = age;
    }
}

void main() {
	Dog d;
	d.test();
	d.math(0);
	d.set_age(10);
}