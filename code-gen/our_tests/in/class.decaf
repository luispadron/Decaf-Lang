class Dog {
    int age;
    string name;

    void set_age(int age) { }
    int get_age() { return age; }
    string get_name() { return name; }
}

void main() {
    Dog d;
    d = New(Dog);

    d.get_name();
    d.get_age();
    d.set_age(10);
}