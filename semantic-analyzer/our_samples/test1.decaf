// this file tests general function name handling and local scoping, this should result in NO errors

int z;

int main(int x) {
    int y;
    x = 3;
    x = add(x, y);
}

int add(int x, int y) {
    return x + y;
}