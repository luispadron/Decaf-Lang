// this file tests all expressions that involve operands

int main() {
    int x;
    int y;
    double d1;
    double d2;
    bool t;
    bool f;

    x + y;
    -x;
    x + z; // error

    d1 + d2;
    -d1;
    x + d1; // error

    t + d1; // error

    UNKWN + d;
    UNKWN + UNKWN;
}