// test array access stuff

void main() {
    int[] nums;
    int[][] numnums;
    double[] floats;
    int x;

    numnums[0][nums[0]];
    x = nums[0] + nums[2];
    x[2.3];

    nums = NewArray(1, int);
    nums = NewArray(1.2, int);
    nums = NewArray(1, string);
    nums = NewArray(1, Undef);
}